# iheartrss.com — environment.
#
# Copy to `.env`. Everything here is validated at boot and the process exits on a
# bad value (plan §9) — a misconfigured deploy should fail loudly at startup, not
# quietly serve wrong absolute URLs for a week.
#
# Only the variables the app currently reads are set below. The rest of §9's
# table (ADMIN_TOKEN, IP_HMAC_KEY_FILE, the revalidation knobs, …) arrives with
# the phase that first uses it.

# Port the server listens on.
PORT=3000

# Public origin. Every absolute URL the app emits (feed links, badge snippets)
# is built from this, so it must match how the site is actually reached.
SITE_URL=https://iheartrss.com

# Hosts that count as "a link back to us" when verifying a submission.
# Point this at your dev origin's host to test the badge check locally.
LINKBACK_HOSTS=iheartrss.com,www.iheartrss.com

# Where the SQLite database lives. Must name a file, not a directory. The parent
# directory is created at boot and write-probed, so a root-owned bind mount fails
# immediately with an actionable message instead of at the first query.
# In the container this is /data/iheartrss.db, backed by the ./data bind mount.
DATABASE_PATH=./data/iheartrss.db


# --- Behind a reverse proxy -------------------------------------------------
# Not read yet — client-IP derivation lands with the submit flow. Documented here
# because getting the second one wrong is a security bug, not a typo.

# Trust X-Forwarded-For. Only ever true when the app is genuinely unreachable
# except through the proxy — which is what the 127.0.0.1 port binding in
# docker-compose.yml buys you.
# TRUST_PROXY=false

# The number of proxies to skip PAST the rightmost X-Forwarded-For entry.
#
# A single nginx in front means 0, NOT 1. nginx's $proxy_add_x_forwarded_for
# (and the Caddy/Traefik equivalents) *appends* the immediate peer, so the app
# sees `<client-supplied garbage>, <real peer>` and the real client is the LAST
# entry. Worked example: with `X-Forwarded-For: 1.2.3.4, 9.9.9.9` and hops=0 the
# client IP is 9.9.9.9. Peeling one more would return 1.2.3.4 — a value the
# client chose — which is a one-header bypass of every rate limit and lets
# anyone poison the stored IP hashes to frame someone else.
# TRUSTED_PROXY_HOPS=0

# --- Outbound fetching (plan §5) --------------------------------------------

# Total budget across every fetch in one submission. Up to 6 requests (plus
# scheme-fallback retries and 5 redirect hops each) share this one clock, so a
# slow site can't hold a synchronous POST past the reverse proxy's own timeout.
SUBMIT_BUDGET_MS=30000

# Per-request sanity cap. The effective timeout is min(this, budget remaining).
FETCH_TIMEOUT_MS=8000

# 5 MB. Exceeding it is an error, never a truncation: a truncated page parses
# fine and merely lacks the link-back, which revalidation would read as a
# deliberate opt-out and delist a member whose homepage just got big.
MAX_RESPONSE_BYTES=5242880


# --- Submissions and moderation (phase 5) -----------------------------------

# Path to a file holding at least 32 random bytes, used to HMAC the truncated
# client IP stored against each submission and report (plan §4).
#
# A FILE, not an env var: an env var sits in `docker inspect`, in dockge's UI, and
# in any .env backed up beside ./data — and the whole scheme rests on this secret,
# because the entire IPv4 space is 2^32 and an unkeyed digest is a rainbow table.
#
# Required in production and validated at boot. In development, a key is generated
# at this path if it doesn't exist. Raw bytes, hex or base64 are all accepted:
#   head -c 32 /dev/urandom > ./data/ip_hmac_key
# IP_HMAC_KEY_FILE=/run/secrets/ip_hmac_key

# Admin token: the credential for /admin (the dashboard, hide/unhide, bans, the
# report queue and the per-domain listing caps).
#
# At least 32 BYTES of hex or base64, validated at boot — a passphrase is refused,
# because nothing else here is a shorter path to full control. Generate one with:
#   openssl rand -hex 32
# If unset, no admin routes are registered at all (they 404, not 401).
#
# It is typed into /admin/login, which exchanges it for a random 32-byte session id
# in an in-memory Map with a 12-hour TTL. The token itself never reaches the browser,
# and sessions are deliberately NOT persisted — a redeploy logs the operator out,
# which is the correct price for not storing session material beside the data (and in
# every backup of it). Login failures are logged and backed off, per IP and globally.
# ADMIN_TOKEN=

# Anti-flood caps (plan §5 Step 7).
#
# MAX_LISTINGS_PER_DOMAIN is per REGISTRABLE domain, resolved with tldts and
# allowPrivateDomains — so alice.github.io and bob.github.io are separate, while
# alice.substack.com and bob.substack.com are not, because the PSL private section
# doesn't cover Substack. The multi-tenant hosts are seeded in the domain_limits
# table at unlimited, and that table (not this variable) is the per-host override.
# MAX_LISTINGS_PER_DOMAIN=5

# The global brake: how many brand-new listings can be created in a rolling 24
# hours. Existing members re-verifying are never refused by it.
# MAX_NEW_LISTINGS_PER_DAY=50

# --- The blog (phase 7, plan §6.4) ------------------------------------------

# Where the markdown posts live. In production this is the read-only bind mount
# from docker-compose.yml (./content on the host → /app/content), so publishing a
# post is `scp` plus nothing — no rebuild, no restart. `content/` is in
# .dockerignore for exactly that reason: the mount is the publishing path, and
# anything baked into the image would be masked by it anyway.
# CONTENT_DIR=/app/content

# How often the loader re-checks the content directory, in milliseconds.
#
# The poll takes max(mtime) across a readdir + stat of every .md file, NOT the
# directory's own mtime: adding or deleting a file bumps the directory mtime, but
# EDITING one does not — measured — so a directory-mtime poll publishes new posts
# fine and then silently refuses to show a typo fix until the container restarts.
# (fs.watch is unreliable across a Docker bind mount, so polling is the shape.)
# CONTENT_POLL_MS=30000

# --- Weekly revalidation (phase 8a, plan §8) ---------------------------------

# The in-process scheduler that keeps the directory honest. Off in dev and in
# tests: NODE_ENV=test disables it outright, so a test run can never reach out to
# a member's server, and `pnpm dev` with this set to false won't either.
# REVALIDATE_ENABLED=true

# Sites per hourly tick. 20/hour × 24 = 480 checks/day, and at a 6-day interval
# that is a steady state of ~2,880 members. WRITE THAT NUMBER DOWN: past it,
# last_checked_at slides permanently past the interval and the /about promise
# quietly becomes false. /healthz reports `oldest_last_checked_at` and
# `overdue_count` so the ceiling is visible before it bites. Note each check is
# 2–4 outbound requests, so 20 sites/hour is really 40–80 requests/hour.
# REVALIDATE_BATCH=20

# How stale a listed site may be before it is re-checked. SIX days, not seven,
# and the difference is load-bearing: /about promises "remove the link and
# you'll be removed within a week", and at 7 the worst case is 7 days plus
# however long until the site's turn comes round — false by a few hours.
# `dropped` is re-checked at 30 days and `removed` at 90; `hidden` never.
# REVALIDATE_INTERVAL_DAYS=6

# Once a page has been seen without the link back, the row moves to this much
# faster cadence until the second sighting confirms it. Without it the first
# sighting lands by day 6 and the confirming one by day 12 — the promise would
# be arithmetically false.
# OPTOUT_FOLLOWUP_HOURS=24

# How long a first sighting stands before it is discarded and the clock restarts.
# Without an upper bound, one innocent 200-without-a-badge (a Cloudflare JS
# interstitial, a mid-deploy window, a stale CDN page) sits there indefinitely and
# months later the first bad tick becomes the confirming sighting.
# OPTOUT_EXPIRY_DAYS=14

# Per-site cooldown on POST /recheck/:id, in minutes. Its own clock
# (`last_recheck_at`), deliberately separate from the scheduler's
# `last_checked_at` — a third party must not be able to reset the cadence the
# removal promise is made of. A recheck can never advance failure_count and can
# never complete an opt-out.
# RECHECK_COOLDOWN_MIN=60

# Pinged after every revalidation batch (healthchecks.io or self-hosted), so a
# scheduler that has quietly stopped ticking is noticed by something other than a
# member emailing to ask why they were never removed. A failed ping is logged and
# nothing more.
# HEALTHCHECK_PING_URL=
